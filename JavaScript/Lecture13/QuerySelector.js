const code = document.querySelector(".navbar");
//querySelector always returns the first element that matches the selector

code.innerText = "Welcome to code";

const blocks = document.querySelectorAll(".block");
console.log(blocks);
